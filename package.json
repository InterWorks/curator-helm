{
  "name": "curator-helm",
  "version": "0.0.0",
  "private": true,
  "description": "Dev tooling for the curator Helm chart (commitlint for conventional-commit CI).",
  "devDependencies": {
    "@commitlint/cli": "19.8.1",
    "@commitlint/config-conventional": "19.8.1"
  }
}
