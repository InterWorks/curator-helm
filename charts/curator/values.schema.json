{
  "$schema": "http://json-schema.org/schema#",
  "type": "object",
  "properties": {
    "environment": {
      "type": "string",
      "enum": ["dev", "qa", "prod"]
    }
  },
  "required": ["environment"]
}